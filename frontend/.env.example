# 这个文件是“环境变量模板”。
# 推荐做法不是直接修改它，而是复制出：
# - .env.development.local
# - .env.production.local
# 然后在复制出来的文件里填你自己的地址。
#
# 为什么要这样做？
# 因为 .env.example 应该始终保持成“示例模板”，
# 方便其他人接手时知道要填哪些变量。

# 如果你希望浏览器直接请求后端完整地址，可以在这里填写：
# 例如：http://127.0.0.1:8080
# 留空时，前端会继续使用 /api 相对路径，并交给 Vite 代理转发。
# 大多数开发联调场景下，这里可以先留空。
VITE_API_BASE_URL=

# 前端构建出来后的静态资源基础路径。
# 本地开发通常保持默认值 / 即可。
# 如果后端要把控制台页面托管到 /console/，构建时就把它改成 /console/。
VITE_APP_BASE=/

# Vite 本地开发代理目标。
# 当前默认值对应“本机模拟后端跑在 8080 端口”。
# 后续切到局域网测试时，把 127.0.0.1 改成队友机器的局域网 IP 即可。
# 前端本周默认使用这个变量切换联调地址，不再手改源码中的请求地址。
# 也就是说：以后如果你问“前端现在在请求谁”，
# 最先看的就是这个变量，而不是去全局搜 axios 地址。
VITE_PROXY_TARGET=http://127.0.0.1:8080
